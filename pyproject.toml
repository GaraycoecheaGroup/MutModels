[project]
name = "MutModels"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.3",
    "scipy>=1.13",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]